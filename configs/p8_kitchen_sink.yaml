# P8 -- everything stacked. NOT an experiment: it changes several things at once
# versus P2, so if it wins you cannot say which part did it. Useful only as a
# "best effort" entry alongside the ladder.
extends: _base.yaml
name: p8_kitchen_sink
backbone: {name: mossformer2_lite, dim: 48, blocks: 7, seq_dim: 192, n_freqs: 129}
head:     {name: eda_conf, n_max: 4, attr_dim: 128, max_speakers: 6, conf_ratio: 0.3}
loss:
  conf_weight: 0.1
  repulsion_weight: 0.1
  overlap_alpha: 1.0
