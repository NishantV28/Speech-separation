# ABLATION: repulsion alone -- does attacking collapse at the source help?
# Identical to p5 except for the single loss term below. Everything else --
# backbone, head, budget, seed, data -- is held fixed, or the delta means nothing.
extends: ../p5_tda_prune_ecapa.yaml
name: p5b_repulsion_only
loss:
  conf_weight: 0.0
  repulsion_weight: 0.0
  overlap_alpha: 0.0
  repulsion_weight: 0.1
