# P0 -- REFERENCE BASELINE, not a contender. Do not report it as a pipeline.
# Over-separation: n_max slots always, count = slots above an energy gate. The
# count IS inferred, so it is a real unknown-speaker method -- just the cheapest,
# and hard-capped at n_max with no mechanism that could ever exceed it.
# Exists so the others have a yardstick.
extends: _base.yaml
name: p0_baseline_oversep
backbone: {name: mossformer2_lite, dim: 48, blocks: 7, seq_dim: 192, n_freqs: 129}
head:     {name: oversep, n_max: 4, gate_db: -25.0}
